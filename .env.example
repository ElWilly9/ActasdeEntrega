# Database — PostgreSQL (produccion) o SQLite (local)
#DATABASE_URL=sqlite:///./inventario.db
DATABASE_URL=postgresql://user:password@host:5432/inventario

# Seguridad — cambiar en produccion
SECRET_KEY=poner-una-clave-segura-aqui
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480
